#!/bin/sh

path=/sys/class/backlight
if [ -d "$path/intel_backlight" ]
then
	file=$path/intel_backlight/brightness
elif [ -d "$path/acpi_video0" ]
then
	file=$path/acpi_video0/brightness
else
	echo "can't find device"
	exit 1
fi

current=$(cat $file)
sudo sh -c "echo $((current + $1)) > $file"
